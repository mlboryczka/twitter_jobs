#!/usr/bin/env bash
# Bootstrap script — installs Postgres 16, Caddy, uv; creates the app user and database.
# See README.md for the full VPS setup walkthrough.
set -euo pipefail

echo "deploy/bootstrap.sh — populated in Phase 7"
